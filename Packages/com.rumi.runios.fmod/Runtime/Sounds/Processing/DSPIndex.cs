using FMOD;

namespace RuniOS.Sounds.Processing
{
    public enum DSPIndex
    {
        head = CHANNELCONTROL_DSP_INDEX.HEAD,
        fader = CHANNELCONTROL_DSP_INDEX.FADER,
        tail = CHANNELCONTROL_DSP_INDEX.TAIL
    }
}
